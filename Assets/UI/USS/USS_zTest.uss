.bg-wold-map-image {
    position: relative;
    top: auto;
    left: auto;
    right: auto;
    bottom: auto;
    width: 100%;
    height: 100%;
    justify-content: center;
    align-self: center;
    flex-grow: 1;
}
